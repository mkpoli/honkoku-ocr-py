# 変更履歴

## 0.3.0（未公開、main）

### 追加
- `--iiif`: IIIF Presentation（v2/v3）のマニフェストからページ画像を取得して処理する。`--pages`でキャンバスを選ぶ。
- PDF入力（`pdf` extra、pypdfium2）。1ページを長辺3,500pxで描画して1コマとして扱う。
- `--page-xml`: PAGE XML（PRImA 2019-07-15）をページごとに書く。
- `--doctor`: ランタイム、実行プロバイダ、モデルキャッシュ、fp32変換の来歴、実効設定を表示する。
- `OCR.process_many`: 複数ページを順に返し、失敗したページは`PageFailure`として続行、`cancelled`で中断できる。
- `--overlap` / `OCR(overlap=True)`: 行の前処理とencoderを別スレッドで先行させる（既定はoff）。
- ベンチマーク: NDLの行データセットによる行認識のCER、みんなで翻刻の翻刻文との見開き単位の突き合わせ、
  読み順の比較（計80コマ）、スレッド設定の一覧、ブラウザ版（WebAssembly、WebGPU）との同一ページ比較。

### 修正
- 行検出の入力への縮小を、ブラウザ版のcanvasと同じ平均化しない双一次補間にした。同じ見開きで検出スコアの差が平均0.09から0.01になった。
- 返り点・送り仮名を含む割書で《割書：》の印が落ちていた。
- モデル取得の一時的な失敗（接続断、5xx、429）を取り直す。
- モデルの取得やセッション作成の失敗は`ModelSetupError`として一括処理を止め、ページごとに繰り返さない。

### 変更
- 出力を書く処理を`output.py`に分け、CLIとベンチマークが共有する。
- CIでmypyによる型検査を行う。`--doctor`は物理コア数を見て、ハイパースレッディングのあるCPUでは測定に基づくスレッド数を示す。

## 0.2.0（2026-09-06）

### 追加
- CPUではfp16 encoderを初回にfp32へ変換してキャッシュし、1行あたり7.3秒から0.86秒になった。来歴ファイルで再利用の可否を判定する。
- `--resume`: 画像・モデル・語彙・設定・版の指紋が一致する完了済みページを飛ばす。
- `--boxes`で行位置を与えて認識だけ行う。与えた座標がそのまま結果に戻る。
- `--preview`で行bboxと読み順を描いたPNGを書く。多ページTIFFのコマ選択（`--frame`）。
- `--threads` / `--decoder-threads` / `--encoder-precision` / `--offline` / `--verify-cache` / `--layout-only`。
- v17とv16fsのモデルにもSHA-256照合を付けた。
- CI: Python 3.10〜3.14でlintとテストを実行し、wheelを検査する。

### 変更
- 出力名は`<stem>__<入力パスのhash>`となり、JSONの行スコアは`detection_confidence`、モデルの版は`--model`で選ぶ。
  `--version`はパッケージの版を表示する。

## 0.1.0

- honkoku-ocr-webの推論パイプラインをPythonとonnxruntimeに移植した初版。
